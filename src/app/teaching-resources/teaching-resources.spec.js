ngDescribe({
    name: 'Teaching Resources',
    modules: ['hrrc-app.teaching-resources'],
    tests: function (deps) {
        it('should pass', function() {
            expect(42).toEqual(42);
        });
    }
});