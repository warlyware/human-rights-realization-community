ngDescribe({
    name: 'Submissions',
    modules: ['hrrc-app.submissions'],
    tests: function (deps) {
        it('should pass', function() {
            expect(42).toEqual(42);
        });
    }
});